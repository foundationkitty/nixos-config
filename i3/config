# Set Win key as Modifier
set $mod Mod4

# Font for window titles. Will also be used by the bar.
font pango:monospace 11

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal -fg white -bg black -fa 'Monospace' -fs 11

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status -c ~/.config/i3/desktop.status
}

include ~/.config/i3/main.conf

